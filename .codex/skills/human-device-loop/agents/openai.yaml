interface:
  display_name: "Human Device Loop"
  short_description: "Facilitate one clear in-app human review journey."
  default_prompt: "Use $human-device-loop in guided-review or harness-conformance mode. Keep one guided journey in one visible session, present one action and optional question at a time, keep protocol internals agent-side, and collect durable evidence without auto-advancing from a heartbeat."
