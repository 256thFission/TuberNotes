interface:
  display_name: "Xcode Loop"
  short_description: "Build, launch, and visually verify TuberNotes."
  default_prompt: "Use $xcode-loop to pin the physical iPad, build, install, launch, and inspect the requested TuberNotes scenario, then return an evidence packet."
